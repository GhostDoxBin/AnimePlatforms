пш# Инструкция по обновлению сайта на GitHub Pages

## ✅ Что уже сделано:
- ✅ Изменения отправлены на GitHub
- ✅ Сайт автоматически обновится через 1-2 минуты

## 📝 Как обновлять сайт в будущем:

### Простой способ (3 команды):

```bash
# 1. Добавить все изменения
git add .

# 2. Создать коммит с описанием
git commit -m "Описание изменений"

# 3. Отправить на GitHub
git push origin main
```

### Примеры описаний коммитов:
- `git commit -m "Исправлена ошибка входа"`
- `git commit -m "Добавлен новый функционал"`
- `git commit -m "Обновлен дизайн"`
- `git commit -m "Исправлена мобильная версия"`

## 🔄 Полный процесс обновления:

1. **Внесите изменения** в файлы проекта
2. **Проверьте изменения:**
   ```bash
   git status
   ```
3. **Добавьте файлы:**
   ```bash
   git add .
   ```
   Или конкретный файл:
   ```bash
   git add index.html
   ```
4. **Создайте коммит:**
   ```bash
   git commit -m "Ваше описание изменений"
   ```
5. **Отправьте на GitHub:**
   ```bash
   git push origin main
   ```

## ⏱️ После отправки:

- GitHub Pages автоматически обновит сайт
- Обычно это занимает **1-2 минуты**
- Проверьте сайт: https://ghostdoxbin.github.io/AnimePlatforms/

## 🚨 Если что-то пошло не так:

### Отменить последний коммит (еще не отправлен):
```bash
git reset --soft HEAD~1
```

### Посмотреть историю коммитов:
```bash
git log --oneline
```

### Откатить изменения в файле:
```bash
git restore имя_файла.html
```

## 💡 Полезные команды:

```bash
# Посмотреть статус
git status

# Посмотреть изменения
git diff

# Посмотреть историю
git log

# Проверить подключение к GitHub
git remote -v
```

