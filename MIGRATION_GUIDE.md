# Руководство по миграции на новую архитектуру

## 📋 Что было исправлено

### 1. Создана единая система конфигурации
- **Файл**: `config.js`
- **Назначение**: Централизованное хранение всех настроек приложения
- **Использование**: `window.Config` доступен глобально

### 2. Создан единый сервис авторизации
- **Файл**: `services/auth-service.js`
- **Назначение**: Унифицированная работа с авторизацией
- **Заменяет**: Дублирующуюся логику из `auth.js` и `main.js`

### 3. Создан единый сервис работы с аниме
- **Файл**: `services/anime-service.js`
- **Назначение**: Единая точка работы с данными аниме
- **Заменяет**: Дублирующуюся логику из `database.js` и `anime-data.js`

### 4. Создана система обработки ошибок
- **Файл**: `utils/error-handler.js`
- **Назначение**: Централизованная обработка и логирование ошибок

## 🔄 Как использовать новые сервисы

### Обновление HTML файлов

Добавьте новые скрипты в начало `<body>` или перед закрывающим `</body>`:

```html
<!-- Конфигурация и утилиты -->
<script src="config.js"></script>
<script src="utils/error-handler.js"></script>

<!-- Сервисы -->
<script src="services/auth-service.js"></script>
<script src="services/anime-service.js"></script>

<!-- Остальные скрипты -->
<script src="database.js"></script>
<script src="anime-data.js"></script>
<!-- ... -->
```

### Использование AuthService

**Вместо:**
```javascript
const user = getCurrentUser();
checkAuth();
```

**Используйте:**
```javascript
const user = window.authService.getCurrentUser();
window.authService.updateAuthUI(
    document.querySelector('.auth-buttons'),
    document.getElementById('admin-link')
);
```

**Или используйте глобальные функции (для обратной совместимости):**
```javascript
const user = getCurrentUser(); // Работает как раньше
checkAuth(); // Работает как раньше
```

### Использование AnimeService

**Вместо:**
```javascript
const anime = window.animeData.getAnimeById(1);
const results = window.animeData.searchAnime('Наруто');
```

**Используйте:**
```javascript
const anime = window.animeService.getAnimeById(1);
const results = window.animeService.searchAnime('Наруто');
```

**Или используйте обратную совместимость:**
```javascript
// window.animeData все еще работает
const anime = window.animeData.getAnimeById(1);
```

### Использование ErrorHandler

```javascript
try {
    // Ваш код
} catch (error) {
    window.errorHandler.handle(error, 'MyComponent.methodName');
}
```

## 📝 Постепенная миграция

### Этап 1: Добавление новых файлов (СДЕЛАНО ✅)
- Добавлены новые сервисы
- Старые файлы продолжают работать

### Этап 2: Обновление существующих файлов
Обновите следующие файлы для использования новых сервисов:

1. **main.js** - уже использует глобальные функции, работает
2. **auth.js** - можно постепенно переходить на `authService`
3. **admin.js** - обновить для использования `animeService`
4. **catalog.js** - обновить для использования `animeService`

### Этап 3: Удаление дублирующегося кода (опционально)
После полной миграции можно удалить:
- Дублирующиеся функции из `main.js` (если все используют `authService`)
- Дублирующиеся методы из `anime-data.js` (если все используют `animeService`)

## ⚠️ Важные замечания

1. **Обратная совместимость**: Старые функции (`getCurrentUser`, `checkAuth`, `window.animeData`) продолжают работать
2. **Постепенная миграция**: Можно обновлять файлы по одному
3. **Тестирование**: После каждого изменения проверяйте функциональность
4. **Резервные копии**: Сохраняйте резервные копии перед большими изменениями

## 🎯 Следующие шаги

1. Обновить `index.html` для загрузки новых скриптов
2. Протестировать авторизацию
3. Протестировать работу с аниме
4. Постепенно обновлять остальные файлы

## 📞 Поддержка

Если возникнут проблемы:
1. Проверьте консоль браузера на ошибки
2. Убедитесь, что все скрипты загружены в правильном порядке
3. Проверьте, что `window.Config` доступен перед использованием сервисов






